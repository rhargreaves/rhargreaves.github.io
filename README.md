rhargreaves.github.io
=====================

## Setup

* Bundler: `$ bundle install`
* Docker: `$ docker build -t blog .`

## Local Server

* Bundler: `$ bundle exec jekyll serve`
* Docker: `$ docker run -v $(pwd):/app -p 4000:4000 -it blog`

